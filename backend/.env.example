# Port the Express server will listen on
PORT=4000

# SQLite database file path (relative to backend/ folder)
DATABASE_PATH=./data/brews.sqlite

# Allowed origin for CORS (your front-end URL)
CLIENT_ORIGIN=http://localhost:5173
